# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg

# Virtual environment
.venv/
venv/
env/

# Database (never commit the live DB — it lives on the Fly.io volume)
*.db
*.sqlite
*.sqlite3

# Secrets
.bookpoint_secret
.env
*.env

# Uploaded files (live on the Fly.io volume)
uploads/

# Runtime data directories (live on the Fly.io volume — never commit)
data/closing_files/
data/commission_carry/
data/journal_learning/
data/error_logs/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Test / cache
.pytest_cache/
.mypy_cache/
htmlcov/
.coverage
coverage.xml

# Debug / patch / deploy scripts that should never be committed
debug_*.py
patch_*.py
fix_*.py
deploy_log*.txt
deploy_log*.log

.ruff_cache/
pytest-cache-files-*/
.tests_passed
.fuse_hidden*
